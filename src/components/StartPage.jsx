import React, { Fragment } from 'react';

const StartPage = (props) => {
    console.log('StartPage')
    return (
        <Fragment>
            <h1>START PAGE</h1>
        </Fragment>
    );
};

export default StartPage;